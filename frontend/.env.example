# Leave blank for the Vite dev proxy / same-origin deployment.
VITE_API_URL=
# Optional tile provider override (retain correct attribution for your provider).
VITE_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
